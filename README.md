# first-action-workflow


name → Workflow name (shown in UI)

on → Trigger condition

push → Runs on code push

jobs → Defines work

runs-on → Runner machine

steps → Tasks inside job

run → Command execution